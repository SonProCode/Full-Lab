# Elasticsearch

[Tiếng Việt](vi/elasticsearch.md) | English

`orders` stores processed business records. `logs-app-*` stores parsed operational logs. Bootstrap installs explicit templates from `elasticsearch/templates`.

Security and TLS are disabled for local teaching only. Production deployments require authentication, TLS, lifecycle policies, snapshots, capacity planning, and multiple fault-domain-aware nodes.
